/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:[
            "images.unsplash.com",
            "uploadthing.com",
            "utfs.io"
        ]
    }
};

export default nextConfig;
